sudo: required
services:
  - docker
language: python
python:
  - "2.7"
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install -r requirements-test.txt  
  - pip install .
script: contester run
